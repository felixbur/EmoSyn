some scripts:

segmentDiphon.pl: script to generate a database from a diphone-label file, 
		a corresponding file with formantfreqs and bandwiths 
		(generated by the esps-tools) and a corresponing file 
		with f0-values (generated by the esps-tools).

filterStartWerte.pl: script to change all time-values in the label file
		into values dividable by 5 (the synthesizer's framesize).

testSenSyn.sh: shellscript for testing.

testphone.sh: shellscript for testing.

viewTracks: tclsh-script to view the parameter-tracks.

sensyn1.1Patch.gz: patch to enable batch-mode with the sensymetrics synthesizer
